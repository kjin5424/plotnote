# 의존성 라이브러리 (가장 중요: 파일 개수가 가장 많음)
node_modules/
/jspm_packages/

# 빌드 결과물
dist/
build/
.next/
out/

# 로그 및 실행 시 생성되는 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# 환경 변수 파일 (보안상 절대 올리면 안 됨)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 에디터 및 OS 설정 파일
.vscode/
.idea/
.DS_Store
thumbs.db